# 大学生制霸生成器

一个纯前端的大学生活经历打卡网页。用户可以为 30 个大学生活项目选择等级，页面会自动计算总分、制霸率和称号，并生成适合截图分享到小红书的结果卡片。

## v1.7 更新

- 区域详情区从两列大卡片改为更轻量的折叠列表，默认展开最强区域；若暂无最强区域则回退展开第一个区域
- 每个区域收拢后只显示区域名、状态、完成率、点亮数量和展开箭头，展开后才显示对应项目 chips，整体更像地图图例/辅助详情
- 降低区域详情的背景、边框和阴影强度，压缩行高与间距，避免区域详情抢走主地图注意力

## v1.6 更新

- 主视觉重构为“非均匀矩形校园分区地图”：整张地图保持完整矩形外轮廓，内部使用 7 个大小不同的矩形分区，不再使用虚线路线、道路节点或 `clip-path polygon`
- 主地图改用 12 列 × 8 行 CSS Grid，按图书馆、教学楼、组织与支线、食堂生活区、宿舍区、操场与远方、校门广场的非均匀矩形布局填满整张校园平面图
- 区域颜色继续按完成率五档染色：`0%` 灰白、`1%-30%` 浅黄、`30%-60%` 橙黄、`60%-90%` 青绿、`90%-100%` 深绿；最强区域保留角标和高亮边框
- 主地图区域保留区域名、完成率、状态短标签和小进度条，区域详情仍放在地图下方，项目 chips 不放回主地图
- 分享海报中的迷你地图同步改为同结构的非均匀矩形分区图，继续作为海报第二视觉中心，并兼顾桌面、平板和手机端不重叠、不横向溢出

## v1.5 更新

- 放弃 `v1.3` / `v1.4` 的抽象染色拼图路线，回到更适合本项目的“校园通关地图 / 副本路线图”视觉
- 主地图恢复为 7 个区域节点的路线图，每个节点显示区域名、完成率、状态和小进度条，并用虚线路线连接；最强区域继续高亮
- 保留 `v1.1` 的页面信息层级顺序：`hero + 结果卡 → 我的大学校园地图 → 大学生活项目 → 分享海报 → footer`
- 区域详情继续放在地图下方，项目 chips 不回到主地图；分享海报也改回精致封面风格，迷你图同步使用小型路线节点图
- 删除 `clip-path polygon` 拼图样式，优先保证桌面和移动端都不重叠、不遮挡、不横向溢出

## v1.4 更新

- 修复主地图和分享海报迷你地图的文字裁切问题，区域结构改为 `shape + label` 双层，`clip-path` 只作用于色块层，不再裁切区域名和完成率
- 7 个区域重新收拢为更接近完整校园轮廓的拼图染色图，缩小块间距、减弱外轮廓、保留统一白色拼缝，避免视觉上像分散色块
- 主地图继续只显示区域名与完成率，最强区域角标保持明显且不被裁切；区域详情、复制文案、下载分享图、重置和 `localStorage` 等既有功能全部保留

## v1.3 更新

- 主视觉重构为“校园地图染色版”，7 个区域改为统一校园外轮廓内的不规则地图色块，页面第一视觉从卡片转为地图
- 主地图只保留区域名与完成率，通过 0%-100% 五档底色表达状态，去掉状态文字、进度条、项目 chips 和 summary
- 最强区域改为明显描边加“最强”角标，区域详情继续保留在地图下方，项目 chips 不再出现在主地图中
- 分享海报重排为“标题 → 制霸率 → 大号迷你校园染色图 → 称号/人格/最强区域 → footer”，弱化已制霸项目摘要
- 保留等级选择、制霸率、称号、人格标签、最强区域、复制文案、下载分享图、重置和 `localStorage`，不改 `ITEMS` 数量与顺序，不引入新库

## v1.2 更新

- 修复主校园地图节点重叠与遮挡问题，移除基于 `absolute positioning` 的区域摆放，改为稳定的 CSS Grid 区域染色布局
- 主地图信息减负，节点只保留区域名、完成率、状态和小进度条，视觉更接近“制霸地图”而不是数据卡片列表
- 新增“区域详情”区，把 7 个区域的项目 chips 下沉到地图下方，避免主地图信息过载
- 分享海报和迷你校园地图同步减负，迷你地图改为简洁区域染色图，已制霸项目摘要最多展示 6 项，整体更接近小红书封面
- 保留 30 项顺序、等级选择、制霸率、称号、人格标签、最强区域、复制文案、下载分享图、重置和 `localStorage` 兼容性

## v1.1 更新

- 优化页面信息层级，调整页面顺序为结果区、主校园地图、项目选择区、分享海报和页脚，让校园地图成为页面主视觉
- 压缩首屏 hero 高度与留白，在常见桌面宽屏下首屏即可看到“我的大学校园地图”标题和地图上半部分
- 强化主校园地图的容器层级、道路连接和最强区域高亮，让地图更接近“制霸地图”而不是附属信息块
- 重排分享海报内容层级，放大并前置迷你校园地图，使其成为制霸率之后的第二视觉中心，同时保持 PNG 导出不裁切
- 减轻项目选择卡片的阴影、留白和按钮高度，保留清晰点击体验但不再抢占地图注意力
- 不新增功能，不改变 `ITEMS` 数量和顺序，不改变 `localStorage` 兼容性，不引入新库

## v1.0 更新

- 主可视化从“大学副本地图”升级为“抽象校园地图”，以 7 个校园区域展示大学生活点亮进度
- `MAP_AREAS` 保留为常量配置，但区域已重构为：校门广场、教学楼、图书馆、食堂生活区、宿舍区、操场与远方、组织与支线
- 分享海报升级为更接近小红书竖图封面的“大学校园制霸报告”，新增 7 区域迷你校园地图
- 保留 30 个项目数量和顺序，不改变等级玩法，继续兼容基于数组索引的 `localStorage` 存档
- 保留制霸率、称号、人格标签、最强区域、复制分享文案、下载分享图和重置功能

## 功能

- 30 个大学生活项目打卡
- 每个项目支持 0-4 五个等级
- 自动计算当前总分、最大总分、制霸率和称号
- 自动生成大学人格标签、最强校园区域、已制霸项目摘要
- 校园分区地图实时展示 7 个区域的完成率、状态与染色结果；区域详情区展示项目 chips
- 分享卡片内置迷你校园分区地图，适合导出 PNG 后分享
- 支持复制分享文案
- 支持下载分享图 PNG
- 支持重置所有选择
- 使用 `localStorage` 自动保存进度，刷新后仍会保留

## 等级

- 0：没体验
- 1：体验过
- 2：熟练
- 3：痛苦精通
- 4：已经麻木

## 使用方法

直接双击 `index.html`，用浏览器打开即可使用。

也可以在项目目录中启动任意静态服务器预览，例如：

```bash
python3 -m http.server 8000
```

然后访问：

```text
http://localhost:8000
```

## 下载分享图

页面会把 `.share-poster` 分享卡片导出为 `campus-conquest-report.png`。

项目默认通过本地文件方式引入 `html2canvas`，不使用 CDN。当前仓库中的脚本路径为：

```text
vendor/html2canvas.min.js
```

如果你需要手动补这个文件，可以：

1. 从 `https://html2canvas.hertzen.com/dist/html2canvas.min.js` 下载文件。
2. 在项目根目录创建 `vendor/` 文件夹。
3. 将文件保存为 `vendor/html2canvas.min.js`。
4. 保持 `index.html` 中的脚本引用不变。

## 文件结构

```text
index.html
style.css
main.js
README.md
vendor/html2canvas.min.js
```

## 技术说明

项目只使用原生 HTML、CSS、JavaScript，不依赖 React、Vue、Node 构建工具、后端服务、外部图片、外部字体或新的第三方库。

主要数据都集中在 `main.js` 顶部常量中：

- `ITEMS`：项目列表
- `LEVELS`：等级列表
- `TITLE_RULES`：称号规则
- `TAG_RULES`：人格标签规则
- `MAP_AREAS`：校园地图区域配置

## License

MIT License
